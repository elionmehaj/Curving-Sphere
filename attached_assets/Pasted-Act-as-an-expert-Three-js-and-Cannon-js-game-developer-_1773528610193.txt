Act as an expert Three.js and Cannon.js game developer. ### [Context]
I want to upgrade my existing 3D endless runner by adding a dynamic, distance-based Level Manager. The game must seamlessly transition between different visual themes and physics states as the player reaches specific distance milestones. Additionally, I need the procedural road generation to become progressively more curvy and challenging the further the player rolls.

[Instructions]
Please write a new LevelManager class or a robust updateEnvironment(distance) function to be called inside my main animation loop. Implement the following distance-based stages:

1. Level 1: Cosmic Space (0m - 499m)

Background & Fog: Keep the current deep space colors.

Track Color: Keep the current neon red.

Gravity: Standard Cannon.js gravity (e.g., world.gravity.set(0, -9.82, 0)).

2. Level 2: Earth Surface (500m - 999m)

Transition Trigger: At exactly 500m.

Background & Fog: Smoothly transition (using THREE.Color.lerp) the scene.background and scene.fog to a bright Sky Blue.

Track Color: All newly generated road chunks from this point forward must be colored vibrant Grass Green.

3. Level 3: Deep Ocean (1000m - 1499m)

Transition Trigger: At exactly 1000m.

Background & Fog: Smoothly transition the scene.background and scene.fog to a murky, dark Deep Blue.

Track Color: All newly generated road chunks must be colored Sandy Yellow.

4. Level 4: The Moon (1500m+)

Transition Trigger: At exactly 1500m.

Background & Fog: Smoothly transition the scene.background to pitch Black, and the scene.fog to a dusty stark White.

Track Color: All newly generated road chunks must be colored Lunar Gray.

Physics Change: Smoothly or instantly change the Cannon.js global gravity to be 50% weaker so the player's jumps and falls feel floaty.

5. Dynamic Curve Difficulty (Progressive Challenge)

Mathematical Curving: Update the road chunk placement logic. Instead of placing chunks in a straight line or randomly, calculate the X-axis position of new chunks using a sine wave function based on the current Z-axis distance (e.g., Math.sin(distance * frequency) * amplitude).

Scaling Difficulty: As the player's total distance increases, dynamically increase the amplitude (making the side-to-side sweeping turns much wider) and increase the frequency (making the curves switch directions much faster and sharper).

[Constraints & Technical Details]
No External Assets: Continue to use only Three.js primitives and basic materials.

Track Chunking Logic: Do not retroactively change the color or position of the road chunks behind the player. Only apply the new colors and sharper curves to the new chunks being spawned ahead.

Smooth Transitions: For the sky and fog, do not instantly snap the colors. Write a small interpolation logic block that fades the colors over a 2 to 3-second window when a milestone is hit.

Output: Please provide the updated logic for the track generation, the animation loop, and the physics world so I can integrate this directly into my existing script.js.